name: Apply recording callback scope patch

on:
  push:
    branches:
      - fix/ptt-media-session

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0

      - name: Apply verified patch
        run: |
          git apply --check Scripts/recording-callback-scope.patch
          git apply Scripts/recording-callback-scope.patch
          rm Scripts/recording-callback-scope.patch
          rm .github/workflows/apply-recording-callback-scope.yml

      - name: Commit patched source
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Sources/ListenToMe/Services/RecordingCoordinator.swift
          git add -u Scripts/recording-callback-scope.patch
          git add -u .github/workflows/apply-recording-callback-scope.yml
          git commit -m "Scope audio callbacks to their recording take"
          git push origin "HEAD:${GITHUB_REF_NAME}"
